// Minimal logging shim for DalaiKarmaBot CLI tests (ESM-only).
// Intentionally quiet and deterministic.

export function enableConsoleCapture() {
  // No-op: capture is handled elsewhere in the full project.
}
